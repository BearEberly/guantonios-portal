name: Deploy reservations demo

on:
  push:
    branches:
      - codex/reservation-pitch
    paths:
      - .github/workflows/reservations-demo.yml
      - pitch/**
  workflow_dispatch:

concurrency:
  group: reservations-demo-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Build and deploy res.beareberly.com demo
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: pitch
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: pitch/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Unit tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Configure Cloudflare Wrangler OAuth
        run: |
          mkdir -p "$HOME/.wrangler/config" "$HOME/.config/.wrangler/config"
          printf '%s' "$CLOUDFLARE_WRANGLER_CONFIG" > "$HOME/.wrangler/config/default.toml"
          printf '%s' "$CLOUDFLARE_WRANGLER_CONFIG" > "$HOME/.config/.wrangler/config/default.toml"
        env:
          CLOUDFLARE_WRANGLER_CONFIG: ${{ secrets.CLOUDFLARE_WRANGLER_CONFIG }}

      - name: Deploy to Cloudflare Pages
        run: npm exec --yes wrangler@latest -- pages deploy dist --project-name res-beareberly --branch codex/reservation-pitch --commit-hash "$GITHUB_SHA"
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
