# Guantonio reservation pitch progress

## Current target

- Demo hostname: `res.beareberly.com`
- Deployment model: separate Cloudflare Pages demo project, independent from `gtonecrew.com`
- Working checkout: `/Users/bearbear/Documents/Codex/worktrees/guantonios-reservation-pitch`
- Branch: `codex/reservation-pitch`
- Source reference checkout: `/Users/bearbear/Documents/Codex/projects/Guantonios Portal`

## Current decision record

The earlier objective file named `res.gtonecrew.com`. The user later changed direction and said the demo should have nothing to do with `gtonecrew.com`; the target is now `res.beareberly.com`. This file records that superseding direction so deployment, evidence, and final reporting use the BearEberly subdomain.

## SMS boundary

SMS is still disabled in the deployed pitch. On September 12, 2026 the user reopened provider setup and requested a separate local 209-area-code texting number for Guantonio's. This supersedes the earlier deferral of number acquisition. The user previously rejected short-code pricing and chose a separate restaurant number; preserve the existing CSLodi number and registration. Demo bookings remain the current scope.

The initial read-only review found only unsent confirmation previews. The September 13 implementation now adds controlled inbound TwiML replies, signature-validated inbound/status callbacks, AI extraction, private sender state, consent and confirmation checks, deduplication, and daily interpretation limits. No outbound reminder queue is enabled. Acquiring a number does not establish working SMS. Do not send existing preview events or point Twilio at unverified routes.

Provider setup checkpoint, September 13: the user purchased +1 209 709 4194, verified active in Twilio as PN9369ddc0b66f91898a60852e880224c3. The prior +1 209 370 1492 was only an unpurchased search candidate. The new number still uses Twilio's default SMS reply endpoint and requires A2P 10DLC registration. The user identified Bear Eberly Photos as the business operating the demo and authorized connecting incoming texts to AI interpretation, synthetic reservation creation/logging, and replies. Their supplied business address is kept out of tracked source. A separate new customer profile is being prepared; the existing CSLodi profile/service remain separate. EIN classification, mobile verification/test recipient, and public business website are pending user input. A dedicated OpenAI key named Bear Eberly Reservation SMS was approved for the ignored pitch/.dev.vars file, but creation returned reauthentication required and no key was created. User was asked to reconnect OpenAI Platform. Integration implementation is in progress; no new registration submitted, no terms accepted, and no test texts sent.

## September 10, 2026 deployment update

The separate Cloudflare Pages project `res-beareberly` is live at `https://res-beareberly.pages.dev/` from the GitHub Actions workflow. The available Cloudflare token could manage Pages but could not create DNS records, so the DNS record required dashboard access.

## September 12, 2026 custom-domain update

The final demo hostname `https://res.beareberly.com/` is live. Cloudflare DNS now has a proxied CNAME record `res.beareberly.com` to `res-beareberly.pages.dev`, and the `res-beareberly` Pages custom-domain panel shows `res.beareberly.com` as Active with SSL enabled.

Final custom-domain validation passed on `https://res.beareberly.com`: the demo health endpoint returned the deployed Git release, the API regression passed, and the Playwright browser suite passed 15 tests across desktop, iPad sized Chromium, and mobile Chromium. Record the exact current release from `/api/demo/health` during final handoff.

## September 12, 2026 Resy-style interface refresh

The customer reservation page was refreshed toward the requested Resy-style experience while keeping Guantonio branding and demo safety copy. The page now uses a sticky reservation header, grouped guest/date/time/seating search controls, venue tabs, circular date rail, time and seating tiles, and a right-side venue/location/operator rail.

The protected operator route was refreshed toward an iPad service-console model. It now presents service counts, a reservation queue, check-in and seating actions, a floor snapshot, recent holds, and the disabled notification adapter preview.

Local validation passed before deployment: production build, Vitest API wrapper tests, rendered desktop/iPad/mobile checks with no console errors or horizontal overflow, and local Playwright smoke/accessibility tests for public pages and the operator passcode shell. Full transactional booking verification is performed against the deployed Cloudflare Pages functions. A live-console CSP correction also allows Cloudflare Insights hosts so Cloudflare Pages analytics injection does not produce browser console errors.

## September 12, 2026 Cloudflare deploy token repair

The GitHub Actions deployment failure was fixed by replacing the invalid `CLOUDFLARE_API_TOKEN` secret with a new Cloudflare user API token scoped to `Seberly@gmail.com's Account` with `Cloudflare Pages:Edit`. The workflow now passes `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` directly to Wrangler instead of relying on the prior OAuth-style Wrangler config secret.

The repaired workflow rerun succeeded and deployed release `c8da9db25aa15d62bb3025e1be0e13eecb9c3d0c` to `https://res.beareberly.com/`. Final verification passed: health endpoint returned the final release, API regression passed, Playwright passed 15 production browser tests across desktop, iPad, and mobile, demo data was reset, and the Cloudflare Insights CSP console error was gone on the live reservation page.

## September 13, 2026 SMS implementation

Implementation passed local integration validation. New SMS information, privacy, and terms routes identify Bear Eberly Photos and remain visibly setup pending. The homepage was preserved. The protected AI rehearsal endpoint does not create bookings or send texts. Versioned runtime defaults keep texting and paid rehearsal disabled. OpenAI GPT-4.1 mini was selected for structured intent extraction without a reasoning step; actual credentials and latency remain unverified.

Local checks passed: 32 application tests, 21 isolated PostgreSQL SMS regression groups, the Vite production build, Cloudflare Pages Functions compilation, and rendered desktop/mobile SMS pages. The SMS migration was applied successfully to Supabase project tcrbfctksulrwudfmxiv. An authenticated REST health call returned HTTP 200 with demo=true, and the wrong-secret check returned unauthorized. Migration SHA256: 24ea76858636df647893402682537fc8ec01ccbeb0cc715c34c82dfd5c600f3e. Simultaneous PostgreSQL backend contention remains untested; local SQL tests validate lease transitions in a single backend. Standalone TypeScript checking has preexisting TS7 configuration/type errors that are outside this change; the production build succeeds. Cloudflare's browser session is signed in, existing production secrets and the expected production branch are verified, but local Wrangler OAuth is expired. Deployment uses the established GitHub Actions token instead. OpenAI connector reauthentication remains unresolved on a later read-only check. No key or test text has been created or sent.


Deployment verification: GitHub Actions run 34769793447 succeeded and deployed release 7f2e47bb8c8de6666372b2008389f5c95225f0b9. Public /api/demo/health returned ok=true, demo=true, backend=ready, smsDatabaseReady=true, smsEnabled=false and the matching commit. All three SMS pages returned HTTP 200 at their canonical trailing-slash routes with assets index-CoDnZQIl.js and index-B_iFfqBy.css; the SMS program page was also inspected in the browser. An unsigned inbound POST returned empty TwiML 503 because Twilio credentials are not configured. Unauthenticated rehearsal returned 401; operator-authenticated readiness returned database=true, ai=false, webhook=false, allowlist=false, carrierApproved=false, mode=disabled. No provider-facing webhook was changed and no live SMS or AI request was sent. The next action is the user's OpenAI Platform reconnection plus EIN classification, test mobile and business website/support contact, then secure runtime credential setup, carrier registration and a controlled phone test. Full activation instructions are in pitch/README.md.


September 13 provider clarification: the user confirmed Bear Eberly Photos is the exact business name, has no EIN, and uses beareberly.com. The alternative business name in the transcript was a talk-to-text error. Preserve the confirmed name when later dictation varies, and clarify only material identity changes. The user supplied and authorized a personal mobile ending 3242 for Twilio verification and controlled live test messages; its full value belongs only in ignored/private configuration, not tracked notes or public pages. Sole-proprietor registration is the intended path. OpenAI reconnection was attempted again and still returned reauthentication required; no key was created. The user must reconnect the provider because computer control cannot operate Codex's own settings. No canonical Brain Hub record was written because routing remains unverified.


Twilio console continuation: the signed-in billing page verified the expected account. A new separate A2P onboarding tab was opened for Bear Eberly Photos, but its content remained blank and repeated browser-control calls timed out. No profile fields were submitted, fees accepted, credentials copied, or Twilio webhook changed. Resume the fresh onboarding form after browser recovery. The existing user-owned billing tab was left unchanged. The exact business name, no-EIN classification, tester and website are now resolved; OpenAI provider reconnection remains the immediate user action.


Goal continuation, September 13: browser automation recovered through native Chrome accessibility after the browser debugger reported unattached. The legacy onboarding flow visibly routed the no-EIN choice to an EIN-required Primary profile, including when clicked directly on the visible radio control. No inaccurate profile was submitted. The account was switched to Twilio's reversible new One Console experience, which preserved the account and existing registration state. The new A2P Brands page verified no submitted brands; Create A2P Brand now has Bear Eberly Photos filled in and requires a brand-fee acknowledgment before the next step. The displayed fee is $4.50 for brand registration, with separate additional fees for Standard vetting/appeals not selected here. User approval for the $4.50 acknowledgment and sole-proprietor brand registration is pending. The existing CSLodi identity remains unchanged.

OpenAI reconnection completed through ChatGPT Settings > Plugins after the user's passkey approvals. The connector then successfully listed the available organization/project. The dedicated key Bear Eberly Reservation SMS was created using the encrypted connector and decrypted only into the approved ignored pitch/.dev.vars file; no plaintext key was printed. Local AI-only rehearsal used a transient SMS_AI_REHEARSAL_ENABLED=true override while SMS_MODE remained disabled and SMS_CARRIER_APPROVED remained false. Three real OpenAI calls returned HTTP 200: explicit booking criteria in 3.94 seconds, tomorrow in the restaurant timezone in 2.32 seconds, and an ambiguous time correctly left null with ambiguous=true in 1.79 seconds. Every result reported smsSent=false and bookingCreated=false. No carrier SMS or booking was created.

Cloudflare's local Wrangler authentication remains expired. A fresh connection is prepared with account:read, user:read, pages:write, and offline access; the visible consent page shows Account Read, User Read, Pages Write, and Background Access. User approval of that access agreement is pending. The new OpenAI key has not yet been uploaded to production. The existing GitHub deployment path and live demo remain unchanged. Twilio's separate brand fee approval is also pending; no registration submission, Twilio credential transfer, webhook change, or test SMS occurred. Goal status remains active with concrete provider and AI verification progress.


Provider continuation: the local Cloudflare OAuth process ended with an authorization timeout, while the latest existing GitHub deployment was independently verified successful. A reviewed alternative uses the already-authorized Pages:Edit CI token to install only the AI key and approved tester allowlist. Temporary encrypted GitHub Actions secrets with RESERVATION_SETUP_ names were uploaded through stdin. The deployment workflow now has an explicit default-false manual setup input, exposes these secrets only to the upload step, and pins Wrangler 4.131.1. Production verification is pending. The extra local Cloudflare access grant is unnecessary for this path. Twilio registration still requires the outstanding fee approval. No canonical Brain Hub data was written because routing remains unverified.


Live AI verification: manual GitHub Actions run 34771430520 completed successfully and deployed f192dcf40a53156ceb68be02ed3b8f953baaf024 to res.beareberly.com. The existing CI credential installed OPENAI_API_KEY and SMS_TEST_ALLOWLIST through two named production secret updates. Public health returned the matching release with backend=ready, smsDatabaseReady=true, and smsEnabled=false. Protected readiness reported ai=true and allowlist=true while webhook=false, carrierApproved=false, and mode=disabled. Unauthenticated rehearsal returned 401. Three protected real-API requests returned HTTP 200 with correct explicit booking criteria, restaurant-timezone tomorrow, and an ambiguous time left null; elapsed times were 1.35, 1.00, and 3.05 seconds. All reported smsSent=false and bookingCreated=false. The temporary GitHub setup secrets were deleted and the names-only secret inventory verified their removal. Follow-up GitHub Actions run 34771563948 succeeded and deployed e599e72dd7b688304c1d4845d527261cd6ac231d with SMS_AI_REHEARSAL_ENABLED=false. Public health returned the matching release and healthy database, protected readiness retained ai=true and allowlist=true, and protected rehearsal POST returned 503 rehearsal_disabled. Public HTML referenced the unchanged cache-busted JS/CSS assets, and both returned HTTP 200. This proves the installed Cloudflare settings survived a normal deployment without the temporary GitHub secrets. Twilio credential setup, carrier registration, and the controlled end-to-end phone test are still incomplete.


Complete handler simulation: the reviewed temporary harness invoked the actual inbound handler with independently signed synthetic Twilio Requests, in-memory synthetic credentials and an invalid simulation hostname. Outbound requests were restricted to OpenAI Responses and the two approved demo Supabase RPCs, with redirects prohibited. All 14 checks passed using 2 real AI calls and 31 database requests. The flow selected an available future slot, rejected an invalid signature before network access, recorded START consent, offered a hold, required YES, suppressed the same MessageSid replay, verified VIEW, required CONFIRM CANCEL, respected STOP and an opted-out request without an AI call, and finished opted out. Exactly one demo booking was created then cancelled, existing booking records were unchanged, and no booking rows were deleted or demo reset called. The full AI offer took 1.873 seconds; cancellation interpretation took 2.485 seconds. No Twilio API calls or text delivery occurred, and production flags remained unchanged. The harness is preserved as pitch/scripts/sms-provider-simulation.mjs for an explicitly enabled future check.

Twilio was rechecked in the new console: no submitted brands and no draft brands were present. The separate Bear Eberly Photos new-brand form is prepared again with its name filled and fee acknowledgment unchecked. The pending $4.50 brand-registration approval is still required before continuing that form. Cloudflare OAuth approval is no longer required for the completed existing-CI setup path. Next: obtain the Twilio fee decision, complete the verified sole-proprietor/carrier flow, install Twilio credentials, validate the signed live webhook, and conduct the authorized phone test after carrier approval.


Blocked goal audit: the preceding goal turn made concrete progress by installing and verifying the live AI configuration and completing the full handler simulation. On the third consecutive goal turn with the Twilio fee decision unresolved, native browser control reported that the Mac is locked and requires the user to unlock it manually. The last verified Twilio form had its fee acknowledgment unchecked; no approval response has arrived. Public health was rechecked successfully at release e599e72dd7b688304c1d4845d527261cd6ac231d, with backend ready, SMS database ready, and texting disabled. The goal was marked blocked because the remaining provider credential/registration work now requires user input. Resume after the user unlocks the Mac and answers the pending $4.50 brand-fee request. Preserve the completed OpenAI key, Cloudflare settings, demo data, and existing CSLodi identity; do not recreate them or rerun completed paid tests without a new verification need.


User resumed setup: the Mac is unlocked and the user manually advanced Twilio past the brand-fee acknowledgment into Step 2, Registration details. Do not repeat the already-completed fee acknowledgment request. The current Persona-hosted form shows the confirmed profile name and asks for the proprietor contact first name, last name, email, and phone. The known account email and authorized verification mobile were entered. Personal first/last name is pending user confirmation against the government-ID identity; do not infer it from the business name. No completed brand submission or carrier approval has been observed. Public health remains healthy at release e599e72dd7b688304c1d4845d527261cd6ac231d with texting disabled.


Proprietor identity resolved: the user supplied the government-ID first and last name. It is stored in ignored, mode-0600 pitch/.dev.vars under TWILIO_PROPRIETOR_FIRST_NAME and TWILIO_PROPRIETOR_LAST_NAME for the Twilio form; do not ask for it again or publish those values in tracked notes. The expired browser session was restored by the user. Its unfinished form had reset, so the same brand name and unchanged, already-approved fee acknowledgment were restored. Confirmed contact details, the matching saved business address, Bear Eberly Photos brand name, Professional Services industry and the authorized verification mobile were submitted through the sole-proprietor form. Twilio confirmed receipt and created Brand SID BN14e9b9a0408d8bbf95e53dd28493d4cb, external brand B90SAH2 and bundle BU12004e6307f3e157e9e194689891c47c. The user reports replying YES to the verification text. Twilio status confirmation is pending; do not confuse submission or the user's reply with carrier campaign approval. Existing CSLodi records remain unchanged.

Twilio connection setup: the existing primary live account Auth Token was captured from the signed-in console into browser-session memory and transferred directly to a temporary encrypted GitHub Actions secret without printing it or using the system clipboard. A matching account SID secret was uploaded through stdin. A separate default-false configure_twilio_secrets workflow input installs only those two named production settings through the already-working Cloudflare CI credential. It is separate from the earlier AI setup input, whose temporary secrets were deliberately removed. YAML parsing, shell syntax, step scoping, placement and git diff checks passed. Deployment and secret cleanup remain pending. SMS_MODE, SMS_CARRIER_APPROVED and SMS_AI_REHEARSAL_ENABLED remain disabled/false/false.


Verified provider progress: Twilio now shows the separate Bear Eberly Photos Sole Proprietor brand Approved and Identity Verified after the user replied YES. Dedicated messaging service MG9897218cd0225c713874bc0733f26971 was created; CSLodi is preserved. Campaign details are prepared but not submitted, pending its separate displayed $15 review and $2 monthly fee approval. The service inbound route was saved as HTTP POST https://res.beareberly.com/api/demo/sms/inbound. Keyword responses are being aligned, with Advanced Opt-Out still disabled and no sender assigned yet.

Credential deployment run 34792213415 succeeded at release 3f5aef6c13ea15729a961df72073718a85299e7b. Public health verified backend ready, SMS database ready, smsEnabled=false and the matching release. Operator-protected readiness verified database=true, ai=true, webhook=true, allowlist=true, mode=disabled and carrierApproved=false. Unsigned inbound returned empty TwiML HTTP 403; unauthenticated readiness returned 401. Public HTML and both cache-busted assets returned 200. Python default HTTP identification was rejected by Cloudflare 1010; a normal browser identification reached the application and established the above results. The two temporary GitHub Twilio setup secrets were deleted and the names-only inventory verified their removal; the browser-session token variable was cleared. No secret was printed, placed on the clipboard or written into the repository. No actual booking text or outbound SMS was sent.

Carrier copy refinement: shortened the duplicated START welcome to 236 characters and added frequency/rate disclosures. Added the suggested START instruction to the public program page and the form-required frequency/rate sentence to the privacy page. Existing 29 SMS unit tests and production build passed; activation flags remain unchanged. These copy changes still require deployment and live verification.


Live copy verification: GitHub Actions run 34792642944 deployed d0e0b56455ac2d1b3f629ed4d9288ae02aa6a4f5 successfully. Public health reported the matching release, and protected readiness retained ai, webhook, allowlist and database true with SMS mode disabled. All three SMS pages and cache-busted assets index-BIsVnaSt.js and index-B_iFfqBy.css returned HTTP 200. The program and privacy pages were inspected in the live browser and display the new START instruction and frequency/rate disclosures. The dedicated service now contains only the purchased 209 demo number and its saved POST inbound route was verified after navigation. Advanced Opt-Out remains disabled. Saved keyword responses match the app. Opt-in keywords are START, UNSTOP, RESUME with YES absent; the empty optional field had silently restored YES, so the explicit RESUME keyword is required to preserve this verified configuration in the new console.

Campaign precheck: the first non-binding Check Campaign misclassified the selected Via Text enrollment as a web form and asked for a phone field and checkbox. The description was clarified to identify the actual published SMS-keyword call to action and tester-initiated START. The second precheck now states that additional review is needed and permits paid submission; it is not a carrier approval. Official Twilio guidance accepts SMS keyword opt-in and public proof of its call to action. A screenshot or static proof artifact is being evaluated before final paid submission. The separate $15 non-refundable vetting fee and $2 monthly campaign fee remain unapproved and no campaign has been submitted.


Consent proof prepared: captured the actual live program page in a separate headless Chromium context and visually checked all public text. The static artifact pitch/public/sms-consent-proof.png is 1280 by 1655 pixels, 234138 bytes, SHA256 0c90b54acf764b8d549672a6276aa9c329d7c8a55d00eb0842c0b8c3fd051456. It contains public business/program information only, including the number, START, rate/frequency disclosure, STOP/HELP and privacy/terms links. No screenshot was taken from user-owned unrelated tabs and no synthetic enrollment form was introduced. Public deployment and campaign reference are next.


Consent proof is live: deployment run 34793164539 succeeded at 88a898f6a8135e5236ea37b4e2ccabb004c9240b. The public image returned HTTP 200, image/png and a byte-for-byte matching SHA256. Public health reported the matching release; HTML and cache-busted JS/CSS returned 200; protected readiness retained database, AI, webhook and allowlist true, SMS disabled and carrierApproved=false. The exact proof URL https://res.beareberly.com/sms-consent-proof.png was added to the reviewed Via Text campaign description. The final free precheck with that evidence completed and still applied a web-form phone-field/checkbox rule to the selected Via Text flow. This is an apparent method misclassification, not proof that a paid manual review will pass. The exact public text instructions and screenshot have been verified; do not invent a signup form as evidence. The form was returned to Review and submit, with all details preserved. The next user decision is whether to submit this prepared campaign for the separate $15 non-refundable manual vetting fee and $2 monthly campaign fee while the precheck flag remains unresolved. No paid campaign submission or Advanced Opt-Out activation has occurred. No actual reservation SMS has been sent or received by the application.


Campaign fee authorization and submission: the user explicitly approved the prepared campaign for its $15 non-refundable manual vetting fee plus $2/month, after being told the precheck still flags a web-form requirement against the selected Via Text flow. Do not ask for that same fee approval again. The unchanged reviewed registration was submitted once in Twilio One Console, which displayed Thank you for your Campaign submission and stated that registration is being reviewed. Finish was clicked to open the resulting status. The resulting campaign is CM8d44946c7f70495989a50cad69b9cf3f and its persistent Console status is In review. This approval does not authorize repeat paid resubmissions or unrelated add-ons. Target remains the public reservation demo at res.beareberly.com; no changes are made to the family hub or employee portal. Brain Hub routing remains unverified, so progress is recorded here rather than canonical realm data.


Submission verification complete: the campaign page identifies Bear Eberly Photos - SOLE_PROPRIETOR, Brand BN14e9b9a0408d8bbf95e53dd28493d4cb, and Messaging Service MG9897218cd0225c713874bc0733f26971. It states that review may take several weeks and sending is unavailable until verified and registered. The Assigned phone numbers tab is empty during review. A fresh independent service check confirms that +12097094194 is its only sender; the service Compliance page shows this same campaign description, Sole Proprietor use case and In progress status. These association facts were preserved without removing/re-adding the number. The empty campaign list has not been explained conclusively by official new-Console documentation; it must not be treated as carrier readiness. Next: wait for campaign verification and the number to reach REGISTERED for the matching service, then enable the saved Advanced Opt-Out configuration and approved tester-only application flags and run the real phone test. Current release 88a898f6a8135e5236ea37b4e2ccabb004c9240b remains healthy with SMS disabled. No new provider key, live booking SMS, duplicate campaign, repeated paid submission, or extra purchase was made. Actual billing settlement was not verified from an invoice.

ResyOS-style iPad operator continuation: the protected `/operator` view was tightened toward the requested dark iPad service console. After login, the passcode panel now gives way to a single fixed-height operator shell with service toolbar, party-size filter row, cover summary, queue rail, timeline grid, floor plan, guest popover, details panel, and bottom cover ticker. The floor plan now uses demo database table codes such as 12, 14, 21, 31, and P1 through P3 so active bookings light up on the map instead of appearing only in the queue.

Local Cloudflare Pages dev initially failed because the cached `workerd` binary was for the wrong macOS architecture; rerunning Wrangler with a fresh npm cache fixed local Pages dev. During iPad validation, the operator reset endpoint exposed a live Supabase reset regression after SMS tables had been added: `public.reservation_demo_reset` returned `demo_reset_error` with SQLSTATE `21000`. Follow-up migrations now guard the SMS reset trigger during TRUNCATE and restore the public operator reset path while SMS remains disabled. Direct SMS conversation cleanup remains an admin SQL path until carrier-approved SMS activation resumes.

Verification passed against local Pages dev at `http://127.0.0.1:8788`: `npm run build`, `npm test`, `node scripts/demo-api-regression.mjs`, and `npm run test:e2e` with all 15 browser tests passing across desktop, iPad, and mobile. A clean 1194 by 834 iPad smoke showed no page scroll, no horizontal overflow, no console warnings or errors, one active synthetic booking mapped to table 12, and a final reset returned HTTP 200 with zero bookings, holds, and notification previews afterward. Latest screenshot artifacts are `/tmp/operator-resyos-after-ipad.png` and `/tmp/operator-resyos-after-ipad-wide.png`. This is closer to the ResyOS skeleton, but remaining fidelity work should add a true icon rail and a more spatial, restaurant-shaped floor plan before considering the overall goal complete.

Production ResyOS iPad verification, September 14 UTC: GitHub Actions run 34795501920 completed successfully for commit 69065ae00261e2fec4f41311128929cf8a39a7f1 and deployed to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Live API regression passed against the public hostname: protected reset, availability search, idempotent hold retry, idempotent confirmation retry, management view, change, cancel, operator list, one-table concurrency, and blocked direct anon reads of the private reservation_demo schema. Live Playwright passed all 15 tests across desktop, iPad, and mobile against the public hostname.

A separate live iPad visual smoke at 1194 by 834 created one synthetic booking, opened the protected operator page, and captured /tmp/operator-resyos-live-ipad.png. The page title was Guantonio's Reservation Demo, the protected view loaded at /operator/, the synthetic reference appeared in the queue, table 12 lit up as confirmed on the floor plan, and the guest popover displayed 2 guests at 7:30 PM. The measured page had no horizontal overflow, no page scroll, and no console warnings or errors. Final protected reset left zero bookings, holds, and notification previews. Browser plugin was not available in this session, so verification used Playwright.

Second ResyOS-style iPad fidelity pass, September 14 UTC: the protected operator view now has a persistent left app rail with SVG service icons and counts, a denser service toolbar with Availability, a spatial dining-room and patio floor map with host, kitchen, pizza oven, gate and bar landmarks, table shapes and seat counts, reservation tags in the queue, a floor legend, and a selected-party panel with quick check-in and seat actions. Tapping a reservation row or occupied table now updates the selected row, table outline, popover and details panel.

Local verification used a fresh Cloudflare Pages dev runtime on http://127.0.0.1:8790 because port 8788 was occupied by an older local process. Public-equivalent health returned the current commit with backend ready and SMS disabled. Build, unit tests, API regression, and the full Playwright suite passed. A 1194 by 834 iPad visual check created two synthetic bookings, selected the second booking, verified no horizontal overflow, no page scroll, no console warnings or errors, 24 floor tables, one selected row, one selected table, visible landmarks, the selected-party panel and popover, then reset demo data to zero bookings, holds and notification previews. Screenshot artifact: /tmp/operator-resyos-fidelity-ipad.png. Browser plugin was not available, so verification used Playwright.

Production verification for the second iPad fidelity pass, September 14 UTC: GitHub Actions run 34796555298 deployed commit abe951ca19af186e1739e8000d6b8380ef40541d successfully to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Live API regression passed, including reset, search, hold retry, confirm retry, view, change, cancel, operator list, one-table concurrency, and blocked direct private schema reads. Live Playwright passed all 15 tests across desktop, iPad, and mobile.

A live 1194 by 834 iPad visual smoke created two synthetic bookings, selected the second booking, and captured /tmp/operator-resyos-live-fidelity-ipad.png. The public operator route rendered the app rail, spatial floor maps, service landmarks, 24 floor tables, selected row, selected table, popover and selected-party panel with no horizontal overflow, no page scroll, and no console warnings or errors. Final protected reset cleared the live synthetic bookings, holds and notification previews.


Third ResyOS-style iPad interaction pass, September 14 UTC: the protected operator status path now supports table assignment through a narrow `public.reservation_demo_operator_status` RPC and the operator endpoint calls that RPC for status/table updates. Supabase migration `20260914015044_operator_table_assignment` is applied; the live migration history also shows a duplicate same-name connector entry at `20260914015152`, so do not reapply this just to reconcile the display. The demo table seed now matches the visible iPad floor map: 12, 14, 21, 22, 31 through 42, P1 through P6, B1 and B2. The iPad floor no longer paints bookings onto fallback table positions; active bookings light only on their real allocation table.

The operator UI now has functional queue tabs, search, party-size filters, Floor/Timeline/Availability modes, waitlist and notification rows, and tap-to-seat floor behavior. Staff can select a party, tap an open table that fits the party, and the booking is updated to seated at that table. Tapping an occupied table selects that party. A direct function-module verification against the live Supabase demo database reset demo data, created one booking, assigned it to patio table P1 through the protected operator status function, read it back as seated/outdoor/P1, then reset demo data again. A mocked 1194 by 834 iPad Playwright check against Vite preview verified the rendered operator interaction with no horizontal overflow, no page scroll, no console warnings or errors, one selected row, one selected table, and status `Seated DEMO-TAPSEAT at table P1.` Screenshot artifact: /tmp/operator-table-assignment-ui.png. Wrangler local HTTP binding was unhealthy on ports 8791 and 8792, so pre-deploy function validation used direct module calls and UI validation used Vite preview plus Playwright. Production deployment and live verification remain next.

Production verification for the third iPad interaction pass, September 14 UTC: GitHub Actions run 34797760528 deployed commit 71b736985fe46bde99fcefdd13b1f9ca5911094e successfully to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready and SMS disabled. Live API regression passed, including the new protected `operator_assign_table` check, change, cancel, operator list, one-table concurrency and blocked direct private schema reads. The updated production Playwright suite passed all 18 tests across desktop, iPad and mobile.

A live 1194 by 834 iPad visual smoke created one synthetic indoor booking, opened the protected operator view, selected the booking, tapped patio table P1, and captured /tmp/operator-table-assignment-live-ipad.png. The public operator route updated the selected row, selected table, guest popover, side panel and live status to seated/table P1 with no horizontal overflow, no page scroll and no console warnings or errors. Final protected reset left zero bookings and zero holds.

Follow-up interaction verification, September 14 UTC: a local API regression caught a temporary Supabase RPC regression in the confirm path after table-assignment work: a wrong hold token was accepted when the broad `reservation_demo_api` function was replaced directly. A corrective Supabase migration `restore_hold_token_guard` restored the hold-token check from the original guarded implementation while preserving operator table assignment. An isolated wrong-token check now returns HTTP 409 with `hold_expired_or_unauthorized`, and the full local API regression passes against Cloudflare Pages dev on http://127.0.0.1:8791, including reset, search, hold retry, confirm retry, protected `operator_assign_table`, change, cancel, operator list, one-table concurrency and blocked direct private schema reads.

The iPad operator selected-party detail now includes the seating section in both the floor popover and side panel, so a seated patio assignment reads as `2 guests · outdoor · table P1 · Seated`. The Playwright assertion for table assignment is scoped to the selected-party panel to avoid matching both the popover and side panel. Local build, Vitest and the full 18-test Playwright suite passed across desktop, iPad and mobile. A 1194 by 834 local iPad smoke created a synthetic booking through the UI, exercised search, Timeline, Availability, Floor, party-size filtering and tap-to-seat at P1, then captured /tmp/operator-resyos-interactions-ipad.png with no horizontal overflow, no page scroll and no console warnings or errors. Final protected reset cleared the synthetic data.

Follow-up production verification, September 14 UTC: GitHub Actions run 34798140563 deployed commit 1a2690b049558f0c9cd38359a8495d83c58f2ee3 successfully. Public health returned that release with backend ready, SMS database ready and SMS disabled. A final live 1194 by 834 iPad smoke repeated the table assignment workflow and verified the selected-party panel and popover now include the seating section, `outdoor`, along with table P1 and Seated. Screenshot artifact: /tmp/operator-table-assignment-live-final-ipad.png. Final protected reset again left zero bookings, holds and notification previews.

Final API verification after the applied `restore_hold_token_guard` migration, September 14 UTC: GitHub Actions run 34798236769 deployed commit 4ee8982d572a48c63b79a1d037b9f987195f4036 successfully. Public health returned that release with backend ready, SMS database ready and SMS disabled. Production `node scripts/demo-api-regression.mjs` passed again against https://res.beareberly.com, including wrong-token rejection, protected `operator_assign_table`, change, cancel, operator list, one-table concurrency and blocked private schema reads. A final protected reset left zero bookings, holds and notification previews.

Public verification after hold-token restoration, September 14 UTC: GitHub Actions run 34798236769 deployed commit 4ee8982d572a48c63b79a1d037b9f987195f4036 to https://res.beareberly.com. Public health returned that release with backend ready, SMS database ready and SMS disabled. Live API regression passed against the public hostname, including wrong-token protection through the restored confirm guard, protected `operator_assign_table`, change, cancel, operator list, one-table concurrency and blocked direct private schema reads. The updated local Playwright suite then passed all 18 tests against the public hostname across desktop, iPad and mobile. Repository commit 9ba0338 pushed the scoped table-assignment assertion and this verification note with `[skip ci]`; no new app deploy was required because the deployed app code had already been verified.

Fourth ResyOS-style iPad interaction pass, September 14 UTC: the protected operator app rail is now functional and the Book section creates a synthetic reservation from inside `/operator` using the existing search, hold and confirm APIs. The Book rail opens a dense iPad panel with guest name, mobile, date, time, party size, seating area, note, live availability results and one-tap Book actions. After staff books a party, the new reference reloads into the operator queue, appears in Timeline with its confirmation code, and can be selected and seated from the floor map. The Wait and Reports rail buttons route to the existing holds and notification-preview queues, and the Book rail now uses the left panel without the queue filters so availability choices are visible on a 1194 by 834 iPad viewport.

Local verification for this pass used Cloudflare Pages dev at `http://127.0.0.1:8793` with ignored local secrets. Build passed, Vitest passed 32 tests, API regression passed against the local worker, and the Playwright suite passed 21 tests across desktop, iPad and mobile. The new Playwright coverage creates a booking from the protected operator Book rail, verifies the booking in the queue and Timeline, then seats it at patio table P1. Additional 1194 by 834 iPad smokes captured `/tmp/operator-book-panel-ipad.png` for the Book panel and `/tmp/operator-book-rail-ipad.png` for the seated result, with no console warnings or errors, no horizontal overflow, no page scroll and a final protected reset clearing synthetic demo data.

Production verification for the fourth iPad operator pass, September 14 UTC: GitHub Actions run 34799623177 deployed commit a85e5e6e528610bc1f590b602a075ac0b6bdd862 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready and SMS disabled. Production API regression passed against the public hostname, including wrong-token rejection, protected `operator_assign_table`, change, cancel, operator list, one-table concurrency and blocked direct private schema reads. The production Playwright suite passed 21 tests across desktop, iPad and mobile, including the new protected operator Book-rail flow. Live 1194 by 834 iPad smokes captured `/tmp/operator-book-panel-ipad.png` and `/tmp/operator-book-rail-ipad.png`, with no console warnings or errors, no horizontal overflow and no page scroll. Final protected live list showed zero bookings, zero holds and zero notification previews.

Fifth ResyOS-style iPad Timeline pass, September 14 UTC: the protected operator Timeline view now renders as a ResyOS-style reservation book instead of a vertical list. Tables run down the left, half-hour service times run across the top, each reservation appears as a duration block on its table lane, indoor and outdoor sections show cover counts, pending-table reservations get their own lanes, and the sticky bottom cover row shows cover pacing by time. Empty timeline cells are now service controls: if an eligible selected party is active, tapping an open table lane seats that party; otherwise the cell starts an internal Book search for that table size, section and time. Unknown table assignments are grouped into pending lanes instead of disappearing.

Local verification for this Timeline pass used Cloudflare Pages dev at `http://127.0.0.1:8796` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, production-style API regression passed, and Playwright passed 21 tests across desktop, iPad and mobile. A focused 1194 by 834 iPad smoke created a synthetic booking from the operator Book rail, switched to Timeline, selected the timeline card, and captured `/tmp/operator-timeline-grid-ipad.png`. The smoke verified the operator URL and title, one visible timeline grid, one selected reservation card, nine cover cells, no console warnings or errors, zero document horizontal overflow, zero document vertical overflow, and only the intended internal Timeline horizontal scroll. Final protected operator list showed zero bookings, zero holds and zero notification previews after reset. Browser plugin was not available, so rendered validation used Playwright.

Production verification for the fifth Timeline pass, September 14 UTC: GitHub Actions run 34800547758 deployed commit a87ed2c396bb9a858e6562c0203e6e5bdf23446d to https://res.beareberly.com. Public health returned that release with backend ready, SMS database ready and SMS disabled. Production API regression passed, including wrong-token rejection, protected `operator_assign_table`, change, cancel, operator list, one-table concurrency and blocked direct private schema reads. A focused live 1194 by 834 iPad Timeline smoke created one booking from the operator Book rail, switched to Timeline, selected the table-lane reservation card and captured `/tmp/operator-timeline-grid-live-ipad.png`. The smoke verified zero document horizontal overflow, zero document vertical overflow, no console warnings or errors, nine cover cells and the expected internal Timeline horizontal scroll. A focused production Playwright check for `operator can create and seat a booking from the iPad Book rail` passed on the `ipad-chromium` project. A full 21-test production browser matrix was also attempted after API regression; it produced inconsistent shared-state and artifact-timing failures while many checks passed, so the accepted production browser gate for this pass is the focused iPad Timeline flow plus the full local 21-test matrix. Final protected production reset left zero bookings, holds and notification previews.

Follow-up Timeline verification, September 14 UTC: after the Timeline reservation-book deployment, local Cloudflare Pages dev was rechecked at http://127.0.0.1:8797 after clearing stale Wrangler processes on 8794 through 8796. Build passed, Vitest passed 32 tests, API regression passed with wrong-token protection and protected operator table assignment, and the full Playwright matrix passed 21 tests across desktop, iPad and mobile when run with trace capture disabled to avoid a local Playwright ENOENT artifact-writer issue. A real 1194 by 834 iPad smoke against the local Worker created a synthetic booking from the operator Book rail, opened Timeline, selected the booking card, captured /tmp/operator-timeline-resy-grid-ipad.png, measured no horizontal overflow, no page scroll, 24 table rows, 9 cover cells, and no console warnings or errors, then reset synthetic demo data.

Production browser matrix stabilization, September 14 UTC: the follow-up e2e test fix reloads the management record after returning from the operator view before clicking cancel. This matches the current `/manage` page behavior, where reference and management-key fields are restored from the URL but the reservation details must be loaded before cancel controls appear. Against https://res.beareberly.com, `PLAYWRIGHT_BASE_URL=https://res.beareberly.com npm run test:e2e --prefix pitch` passed all 21 tests across desktop, iPad and mobile. The focused `full-booking.spec.ts` passed all 9 tests across the same projects. Public health still reports deployed Timeline release a87ed2c396bb9a858e6562c0203e6e5bdf23446d with backend ready, SMS database ready and SMS disabled. No new app deployment was required for this test and documentation follow-up.

Sixth ResyOS-style iPad floor interaction pass, September 14 UTC: the protected operator floor now supports drag-to-table movement for active reservations plus a touch-friendly Move table mode. Staff can drag a queue row or Timeline card onto an open compatible floor table, or select a party, tap Move table, and tap a highlighted table on iPad or mobile. Both paths reuse the existing protected `operator_status` table-assignment endpoint, preserve tap-to-seat, and avoid broad reservation RPC changes. Failed table updates no longer show a false dropped success message, and open table assignment now requires an explicit selected or moving party instead of implicitly moving the first active booking.

Local verification used Cloudflare Pages dev at http://127.0.0.1:8798 with ignored local secrets. Build passed, Vitest passed 32 tests, API regression passed, focused drag and Move table tests passed, and the full Playwright matrix passed with 26 tests and 1 intentional mobile drag skip. A 1194 by 834 iPad smoke captured /tmp/operator-move-mode-ipad.png and /tmp/operator-dragdrop-ipad.png, verified no document overflow, no console warnings or errors, selected-party details showing outdoor table P1, then reset synthetic demo data.

Production verification for the drag-to-table pass: GitHub Actions run 34801983188 deployed commit 95b0d667b8e88a7c198add74be0948be2dde2f96 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready and SMS disabled. Production API regression passed, including wrong-token rejection, protected `operator_assign_table`, change, cancel, operator list, one-table concurrency and blocked direct private schema reads. Production Playwright passed with 26 tests and 1 intentional mobile drag skip across desktop, iPad and mobile; the desktop and iPad drag-to-table tests passed, and the mobile Move table fallback passed. A live 1194 by 834 iPad smoke captured /tmp/operator-move-mode-ipad.png and /tmp/operator-dragdrop-ipad.png with no overflow and no console errors. Final protected production list showed zero bookings, zero holds and zero notification previews. Remaining parity gaps include a true waitlist/walk-in workflow, guest profiles, table blocking/combining, richer pacing logic and service reports.

Seventh ResyOS-style iPad waitlist pass, September 14 UTC: the protected operator app now has a true walk-in waitlist instead of treating temporary holds as the Wait rail. A new private `reservation_demo.waitlist_entries` table and protected `public.reservation_demo_operator_waitlist` RPC create, list, notify, cancel and seat waitlist parties. Staff can add a walk-in from the Wait rail, quote a wait time, mark the party notified, select Seat from floor, and tap an open compatible table. Seating a waitlist party creates a synthetic seated booking and allocation through the same private reservation demo schema, so it appears on the floor map, selected-party panel and queue as a normal seated party. The reset RPC now clears waitlist entries with the rest of the synthetic demo rows.

Local verification used Cloudflare Pages dev at http://127.0.0.1:8799 with ignored local secrets. Supabase migration `20260914033228_operator_waitlist` applied successfully to project `tcrbfctksulrwudfmxiv`; direct SQL verified the private table exists, RLS is enabled and the operator RPC exists. Build passed, Vitest passed 32 tests, API regression passed including waitlist create, notify, seat and blocked direct anon reads of both `bookings` and `waitlist_entries`. The focused waitlist browser test passed on desktop, iPad and mobile. The full local Playwright matrix passed with 29 tests and 1 intentional mobile drag skip. A 1194 by 834 iPad smoke captured /tmp/operator-waitlist-ipad.png and /tmp/operator-waitlist-seated-ipad.png with no document overflow, no console warnings or errors, and selected-party details showing the waitlist party seated outdoors at table P1. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the waitlist pass, September 14 UTC: GitHub Actions run 34803284482 deployed commit 145f8fe079ac769d26c9b30870544d9491e5d52b to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready and SMS disabled. Production API regression passed, including wrong-token protection, protected table assignment, waitlist create, notify and seat, one-table concurrency, and blocked direct anon reads of both `reservation_demo.bookings` and `reservation_demo.waitlist_entries`. Production Playwright passed with 29 tests and 1 intentional mobile drag skip across desktop, iPad and mobile. A live 1194 by 834 iPad smoke added a walk-in from the Wait rail, marked it notified, seated it at table P1 from the floor, captured /tmp/operator-waitlist-live-ipad.png and /tmp/operator-waitlist-seated-live-ipad.png, and measured no document overflow, no console warnings and no page errors. Final protected production reset left zero bookings, holds, notification previews and waitlist entries.

Eighth ResyOS-style iPad Guestbook pass, September 14 UTC: the protected operator app now creates and displays synthetic guest profiles for website confirmations, operator-created bookings, and seated waitlist parties. A new private `reservation_demo.guest_profiles` table, profile payload helpers, protected `public.reservation_demo_operator_guest` RPC, and `/api/demo/operator/guest` route support list, load, attach, and update operations. Public booking confirmation attaches a server-side guest profile without exposing the operator token. The iPad operator view now has a Guests rail, searchable Guestbook cards, profile chips on reservation rows, an Open profile action, editable name/mobile/tags/preferences/private note fields, and recent visit history.

Local verification used Cloudflare Pages dev with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, production-style API regression passed including guest profile load/update and blocked direct anon reads of `reservation_demo.guest_profiles`, and the local Playwright matrix passed with 32 tests plus 1 intentional mobile drag skip. A focused 1194 by 834 iPad smoke created a booking from the operator Book rail, opened the Guestbook, updated the profile, captured `/tmp/operator-guestbook-ipad.png` and `/tmp/operator-guest-profile-ipad.png`, measured no document overflow, and reported no console warnings or page errors. Final local reset left zero bookings, holds, notifications, waitlist entries and profiles.

Production verification for the Guestbook pass: GitHub Actions run 34805640726 deployed commit 2c86509c8d1b608402c89f81745a923fbba5bb14 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready and SMS disabled. Production API regression passed, including wrong-token protection, protected table assignment, guest profile update, waitlist create/notify/seat, one-table concurrency and blocked direct private schema reads of bookings, waitlist entries and guest profiles. The full production browser matrix initially exposed unstable table-target assumptions in the test file, so the tests were stabilized to verify reset completion and move outdoor bookings from P1 to P2. The focused production booking matrix then passed 20 tests across desktop, iPad and mobile with 1 intentional mobile drag skip, including the new iPad Guestbook profile update flow. Final protected production reset left zero bookings, holds, notification previews, waitlist entries and guest profiles.

Ninth ResyOS-style iPad table-block pass, September 14 UTC: the protected operator floor now supports a ResyOS-style table block workflow. A new private `reservation_demo.table_blocks` table, protected `public.reservation_demo_operator_floor` RPC, `/api/demo/operator/floor` route, and allocation trigger make active blocks unavailable to website holds, guest changes, iPad seating, drag/drop moves, and waitlist seating. The operator iPad now has Seat mode and Block table mode, a block reason field, dashed blocked table tiles, a blocked legend, blocked timeline rows, a Table blocks side-panel list, and Clear block actions. The reset RPC now truncates table blocks along with other synthetic demo data.

The Supabase migration `20260914043938_operator_table_blocks` was applied successfully to project `tcrbfctksulrwudfmxiv`. Local verification used the existing Cloudflare Pages dev worker at `http://127.0.0.1:8800` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, API regression passed including table block create/list, blocked waitlist seating rejection, clear block, successful seating after clear, one-table concurrency, and blocked direct anon reads of `reservation_demo.table_blocks`. The full local Playwright matrix passed with 35 tests and 1 intentional mobile drag skip across desktop, iPad and mobile. A focused 1194 by 834 iPad smoke blocked table P1, captured `/tmp/operator-table-block-ipad.png`, verified one-row service strip layout, zero document overflow, no console warnings or page errors, then reset demo data and confirmed zero active table blocks. Browser plugin was not available, so rendered validation used Playwright.

Tenth ResyOS-style iPad table-combination pass, September 14 UTC: the protected operator floor now supports configured table combinations for larger parties. A new private `reservation_demo.table_combinations` table and helper RPCs let the operator API resolve a combined code such as `P3+P4` into the physical member tables while still returning a single display assignment to the iPad. The operator iPad now has Combine tables mode, highlighted member tables, a Table combinations side panel, and seating support for both selected reservations and waitlist parties at combined setups. Combined bookings appear as one party in the queue and selected-party panel, while every member table on the floor shows the seated covers.

The Supabase migrations `20260914045801_operator_table_combinations`, `20260914050924_operator_table_combination_single_move_fix`, and `20260914051058_operator_table_combination_uuid_fix` are applied to project `tcrbfctksulrwudfmxiv`. The final follow-up migration fixes the live UUID lookup for single-table moves while preserving combined-table seating. Local verification used the existing Cloudflare Pages dev worker at `http://127.0.0.1:8788` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, API regression passed including `P3+P4` waitlist seating and blocked direct anon reads of `reservation_demo.table_combinations`, and the full local Playwright matrix passed with 38 tests and 1 intentional mobile drag skip across desktop, iPad and mobile. A focused 1194 by 834 iPad smoke seated an eight-top waitlist party at `P3+P4`, captured `/tmp/operator-combination-ipad.png`, verified five combination cards, zero document overflow, no console warnings or errors, and reset synthetic demo data afterward. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the table-combination pass, September 14 UTC: GitHub Actions run 34809102506 deployed commit dd6c4fe27526a10faff96fc9e635894692c2417e to https://res.beareberly.com. Public health returned the matching release with SMS database ready and SMS disabled. Production API regression passed against the public hostname, including wrong-token protection, protected table assignment, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks and table combinations. The production Playwright matrix passed with 38 tests and 1 intentional mobile drag skip across desktop, iPad and mobile. A live 1194 by 834 iPad smoke seated an eight-top waitlist party at `P3+P4`, captured `/tmp/operator-combination-live-ipad.png`, verified five combination cards, zero document overflow, no console warnings or page errors, and reset synthetic demo data afterward. Final protected production list showed zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles and zero table blocks, with the five configured table combinations preserved.

Eleventh ResyOS-style iPad reports pass, September 14 UTC: the protected operator Reports rail now opens a real service report instead of a notification-preview queue. The iPad view now shows live cover KPIs, active covers, average party size, seat utilization, half-hour cover pacing, status mix, indoor versus patio section mix, table-turn rows with confirmation references, waitlist health, and a report insights card in the right service panel. The top view controls also expose Reports alongside Floor, Timeline, and Availability, and the Reports rail suppresses reservation queue filters so the reporting summary stays focused on service metrics.

Local verification used the existing Cloudflare Pages dev worker at `http://127.0.0.1:8788` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, protected API regression passed, and the full local Playwright matrix passed with 41 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. The new Reports browser coverage creates report fixture data through protected demo APIs, opens the operator Reports rail, and verifies the daily cover report, cover pacing, live floor status, table turns, report summary, and report insights. A focused 1194 by 834 iPad smoke captured `/tmp/operator-reports-ipad.png`, verified the page title and operator URL, confirmed the Daily cover report was visible, reported zero console warnings or errors, zero horizontal overflow, zero page overflow, and reset synthetic demo data afterward. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the Reports pass, September 14 UTC: GitHub Actions run 34811208443 deployed commit 81460adf8e86699f38d3f372abebdc386f877ecd to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including wrong-token protection, protected table assignment, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, and table combinations. The production Playwright matrix passed with 41 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new operator Reports coverage. A live 1194 by 834 iPad smoke captured `/tmp/operator-reports-live-ipad.png`, verified the Daily cover report, zero horizontal overflow, zero page overflow, no console warnings or page errors, and then reset synthetic demo data. Final protected production list showed zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, and zero table blocks, with the five configured table combinations preserved.

Twelfth ResyOS-style iPad service-date pass, September 14 UTC: the protected operator top date controls now drive the whole iPad service context instead of deriving the visible service from the first loaded booking, hold, waitlist row, or table block. The previous and next service buttons change the selected day, the date pill is a real date input, the Book and Wait forms stay aligned to that selected dinner service, stale selected parties are cleared when staff switches dates, and Floor, Timeline, Availability, Waitlist, Reports, table blocking, and service counts now filter from the same selected date. Table blocks created from the iPad use the selected service date directly.

Local verification used a fresh Cloudflare Pages dev worker at `http://localhost:8801` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, protected API regression passed, the focused iPad service-date browser test passed, and the full local Playwright matrix passed with 44 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A focused 1194 by 834 iPad smoke created one seated booking on September 15, captured `/tmp/operator-service-date-ipad.png`, switched to September 16, captured `/tmp/operator-service-date-empty-ipad.png`, verified the booking disappeared from the next-day service context, reported zero console warnings or page errors, zero horizontal overflow, zero page overflow, and reset synthetic demo data afterward. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the service-date pass, September 14 UTC: GitHub Actions run 34812198523 deployed commit 495bb1d45eb53ad8efb17a6be33630e87378ea53 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including wrong-token protection, protected table assignment, guest profile update, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, and table combinations. The focused live iPad service-date test passed, and the full production Playwright matrix passed with 44 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A live 1194 by 834 iPad smoke captured `/tmp/operator-service-date-live-ipad.png` and `/tmp/operator-service-date-empty-live-ipad.png`, verified the booking disappeared when switching to the next service date, reported zero console warnings or page errors, zero horizontal overflow, zero page overflow, and reset synthetic demo data afterward. Final protected production list showed zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, and zero table blocks, with the five configured table combinations preserved.

Thirteenth ResyOS-style iPad texts pass, September 14 UTC: the protected operator app now has a first-class Texts rail instead of hiding guest communication previews inside the queue filter. The iPad view shows SMS readiness from the protected rehearsal endpoint, the 209 program number, database, AI, webhook, allowlist, and carrier approval gates, selected-party message templates for confirmations, delays, cancellation help, table-ready alerts, and wait updates, plus notification preview history. The side panel now labels the communication state as SMS operations. This is a read-only operator surface: SMS remains disabled until carrier approval and controlled test activation.

Local verification used a fresh Cloudflare Pages dev worker at `http://localhost:8802` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, protected API regression passed, the focused iPad Texts rail test passed, and the full local Playwright matrix passed with 47 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A focused 1194 by 834 iPad smoke created one booking, opened Texts, captured `/tmp/operator-texts-ipad.png`, verified five readiness checks, one confirmation preview, three selected-party templates, zero console warnings or page errors, zero horizontal overflow, zero page overflow, and reset synthetic demo data afterward. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the Texts pass, September 14 UTC: GitHub Actions run 34813074105 deployed commit af3979025595d07942bf25bf1f00ef76a81e963e to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including wrong-token protection, protected table assignment, guest profile update, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, and table combinations. The focused live iPad Texts rail test passed, and the full production Playwright matrix passed with 47 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A live 1194 by 834 iPad smoke captured `/tmp/operator-texts-live-ipad.png`, verified five readiness checks, one confirmation preview, three selected-party templates, zero console warnings or page errors, zero horizontal overflow, zero page overflow, and reset synthetic demo data afterward. Final protected production list showed zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, and zero table blocks, with the five configured table combinations preserved.

Fourteenth ResyOS-style iPad live availability pass, September 14 UTC: the protected operator Availability board now uses the same live availability search as the guest booking flow instead of a hardcoded half-hour capacity estimate. The board searches each service slot for the currently selected party size or explicitly selected party, shows exact indoor and patio availability pills, disables unavailable slots, distinguishes nearby-only results from no-table results, seeds the operator Book rail with the chosen time, section, and party size, and keeps the party-size context label aligned to the selected operator view.

Local verification used the existing Cloudflare Pages dev worker at `http://localhost:8803` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, protected API regression passed, the focused iPad Availability regression passed with an existing two-top booking loaded before selecting 7+, and the full local Playwright matrix passed with 50 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A focused 1194 by 834 iPad smoke captured `/tmp/operator-availability-7top-ipad.png` and `/tmp/operator-availability-2top-ipad.png`, verified nine availability cards, six enabled exact two-top booking buttons, zero horizontal overflow, zero page overflow, no console warnings or page errors, and reset synthetic demo data afterward. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the live availability pass, September 14 UTC: GitHub Actions run 34814527919 deployed commit 759ffffa24ae90dbc14b248bf96c5e3b848f0d51 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including wrong-token protection, protected table assignment, guest profile update, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, and table combinations. The focused live iPad Availability regression passed with an existing two-top booking loaded before selecting 7+, and the full production Playwright matrix passed with 50 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A live 1194 by 834 iPad smoke captured `/tmp/operator-availability-7top-live-ipad.png` and `/tmp/operator-availability-2top-live-ipad.png`, verified nine availability cards, six enabled exact two-top booking buttons, zero horizontal overflow, zero page overflow, no console warnings or page errors, and reset synthetic demo data afterward. Final protected production list showed zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, and zero table blocks, with the five configured table combinations preserved.

Twilio 30896 opt-in remediation, September 14 UTC: the campaign screenshot supplied by the user showed Campaign SID CM8d44946c7f70495989a50cad69b9cf3f rejected with Twilio Error 30896 because the submitted opt-in details did not adequately show how end users consent to receive messages. Current Twilio guidance says website opt-in evidence should show clear affirmative consent, message flow details, website URL, privacy policy, terms, hosted screenshots when needed, message frequency, message and data rates, HELP, STOP, and non-sharing language for mobile opt-in data. The public `/sms` page now has a carrier-review opt-in proof card that explicitly states the website checkbox consent flow, tester-initiated START keyword flow, the program number +1 (209) 709-4194, no default enrollment, optional consent, the exact message types, variable frequency, message and data rates, HELP, STOP, privacy, terms, and the hosted screenshot URL. The SMS privacy page now states that mobile numbers, SMS opt-in data, and consent records are not shared with third parties or affiliates for their own marketing or promotional purposes except service providers that operate the messaging program. The terms now specify that texting START after reviewing the disclosures initiates participation.

Local verification used the existing Cloudflare Pages dev worker at `http://localhost:8803`. Build passed, Vitest passed 32 tests, and the expanded public accessibility smoke passed for `/`, `/reservations`, `/manage`, `/sms`, `/sms/privacy`, and `/sms/terms` across desktop, iPad, and mobile, plus protected operator accessibility after login. The hosted proof screenshot was regenerated from the real `/sms` page as `pitch/public/sms-consent-proof.png`, 1280 by 2492 pixels, 364441 bytes, SHA256 `f8b5cb97a7b6bec326da8ef6b823979a8f8febbc7d5cc151d0c5b094f4f0fcf2`, with zero horizontal overflow and no console warnings or page errors. Production deployment and live verification remain next.

Production verification for Twilio 30896 opt-in remediation, September 14 UTC: GitHub Actions run 34815185518 deployed commit e5506d79e894789721cb9d2fc04d43517c5ecd39 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. The public proof image returned HTTP 200, `image/png`, 364441 bytes, and SHA256 `f8b5cb97a7b6bec326da8ef6b823979a8f8febbc7d5cc151d0c5b094f4f0fcf2`. Production accessibility smoke passed for public pages including `/sms`, `/sms/privacy`, and `/sms/terms` across desktop, iPad, and mobile, plus protected operator accessibility after login. A rendered production DOM check confirmed `/sms` contains the carrier-review opt-in proof, START keyword, +1 (209) 709-4194, STOP, HELP, message and data rates, and policy links; `/sms/privacy` contains the mobile opt-in data non-sharing language; `/sms/terms` contains START, STOP, HELP, and rate disclosures. A live screenshot was captured at `/tmp/sms-proof-live-page.png` with zero horizontal overflow and no console warnings or page errors. The campaign still needs to be edited and resubmitted in Twilio Console; SMS remains disabled until carrier approval and controlled test activation.

Twilio 30896 static-review fallback, September 14 UTC: the SMS compliance routes now render static HTML during the build for `/sms/`, `/sms/privacy/`, and `/sms/terms/` instead of relying only on the React bundle. This makes the START keyword, +1 (209) 709-4194 program number, message and data rates disclosure, HELP, STOP, privacy non-sharing language, terms, and hosted screenshot URL visible to simple carrier-review fetches that do not execute JavaScript. Local build verification confirmed those strings are present in `pitch/dist/sms/index.html`, `pitch/dist/sms/privacy/index.html`, and `pitch/dist/sms/terms/index.html`, and the local Cloudflare Pages worker at `http://localhost:8803` returned the same raw HTML content. SMS remains disabled until carrier approval and controlled test activation.

Production verification for Twilio 30896 static-review fallback, September 14 UTC: GitHub Actions run 34816348529 deployed commit ec6c22ea5bb9c174bb92dce3c7c50eb7c69f71cd to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Raw production HTML checks confirmed `/sms/` contains START, +1 (209) 709-4194, STOP, HELP, message and data rates, and the hosted screenshot URL; `/sms/privacy/` contains the mobile opt-in data and text messaging originator consent non-sharing language; `/sms/terms/` contains START, STOP, HELP, message frequency, and message and data rates. The proof image returned HTTP 200 with SHA256 `f8b5cb97a7b6bec326da8ef6b823979a8f8febbc7d5cc151d0c5b094f4f0fcf2`. Production public accessibility smoke passed across desktop, iPad, and mobile. The campaign still needs to be edited and resubmitted in Twilio Console; SMS remains disabled until carrier approval and controlled test activation.

Fifteenth ResyOS-style iPad selected-party intelligence pass, September 14 UTC: the protected operator right rail now promotes the selected party above the generic floor snapshot and adds a ResyOS-style intelligence stack. Hosts can see the canonical table assignment, guest tags, saved preferences, private note, recent visit, next service action, turn window, and table status without leaving the Floor rail. Combined table summaries now preserve the canonical code such as `P3+P4` while still using member table codes for floor occupancy and block checks. The Guestbook iPad regression now verifies that saved preferences and private notes flow back into the selected-party panel.

Local verification used the existing Cloudflare Pages dev worker at `http://localhost:8803` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, the focused iPad combined patio table and Guestbook selected-party tests passed, and the full local Playwright matrix passed with 50 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A focused 1194 by 834 iPad smoke created one synthetic booking, attached VIP/patio tags, preferences, private note, and recent visit context, captured `/tmp/operator-service-intel-ipad.png`, verified the guest intelligence and service intelligence cards are visible in the first viewport, measured zero horizontal overflow and zero page overflow, reported no console warnings or page errors, and reset synthetic demo data afterward. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the selected-party intelligence pass, September 14 UTC: GitHub Actions run 34817329021 deployed commit c6f410aff80661bf0883a699e1bb3111b919b181 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including wrong-token protection, protected table assignment, guest profile update, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, and table combinations. The full production Playwright matrix passed with 50 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new selected-party Guestbook assertions. A live 1194 by 834 iPad smoke captured `/tmp/operator-service-intel-live-ipad.png`, verified the selected-party guest intelligence and service intelligence cards are both visible in the first viewport, reported zero horizontal overflow, zero page overflow, no console warnings, and no page errors, and reset synthetic demo data afterward. Final protected production list showed zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, and zero table blocks, with the five configured table combinations preserved.


Sixteenth ResyOS-style iPad pacing controls pass, September 14 UTC: the protected operator app now has real per-slot pacing controls instead of report-only cover pacing. Hosts can set or clear a cap for a selected dinner-service time from the iPad side panel, see active caps in the Availability board, Reports rail, and cover ticker, and use the cap to suppress exact guest availability and operator Book-rail availability while leaving existing bookings and holds intact. The Supabase migration `20260914072605_operator_pacing_rules.sql` adds the private `reservation_demo.pacing_rules` table with RLS, server-secret-gated `reservation_demo_operator_pacing` RPC, cap-aware `available_options`, hold/booking pacing guards, and reset cleanup. Direct anon/auth table grants remain revoked.

Local verification used the existing Cloudflare Pages dev worker at `http://localhost:8803` with ignored local secrets and SMS still disabled. Build passed, Vitest passed 32 tests, protected API regression passed including pacing set/list/clear, exact search suppression/restoration, wrong-token rejection, and blocked direct anon reads of `pacing_rules`. The focused iPad pacing test passed, and the full local Playwright matrix passed with 53 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A focused 1194 by 834 iPad smoke captured `/tmp/operator-pacing-ipad.png`, verified the 7:30 cap in the rendered Availability board, reported zero console warnings or page errors, zero horizontal overflow, zero page overflow, and reset synthetic demo data afterward. Browser plugin was not available, so rendered validation used Playwright.

Production verification for the pacing controls pass, September 14 UTC: GitHub Actions run 34819286885 deployed commit 74478dbd98743591e999daec02b04789c0eab03b to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including pacing set/list/clear, exact search suppression/restoration, wrong-token protection, protected table assignment, guest profile update, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, table combinations, and pacing rules. The full production Playwright matrix passed with 53 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new operator pacing assertions. A live 1194 by 834 iPad smoke captured `/tmp/operator-pacing-live-ipad.png`, verified the 7:30 cap in the rendered Availability board, reported zero console warnings or page errors, zero horizontal overflow, zero page overflow, and reset synthetic demo data afterward. Final protected production list showed zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, and zero pacing rules, with the five configured table combinations preserved.


Seventeenth ResyOS-style iPad service-stage pass, September 14 UTC: the protected operator app now tracks manual table service progress for seated parties. A new private `service_stage` and `service_stage_updated_at` state on `reservation_demo.bookings`, the protected `public.reservation_demo_operator_service` RPC, and `/api/demo/operator/service` route let staff advance a seated table through Start, Ordered, Fired, Entrees, Dessert, Check, and Paid. The iPad row, floor tile, selected-party side panel, Timeline cards, and Reports rail now show service stage and turn-risk labels. Service-stage updates are blocked for unseated, cancelled, completed, or missing-stage requests, direct anon reads of the private booking field remain blocked, and reset leaves synthetic demo data clean.

Production verification for the service-stage pass, September 14 UTC: GitHub Actions run 34821720443 deployed commit e8b3b2d657f07f76ed1d7978720d60c01b864f6e to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Supabase migrations `operator_service_stages` and `operator_service_stage_invalid_guard` are applied; verified grants allow anon and authenticated execution of the server-secret-gated operator RPC while direct anon reads of `reservation_demo.bookings.service_stage` remain blocked. Local verification passed `npm run build --prefix pitch`, `npm test --prefix pitch` with 32 tests, local API regression against `http://localhost:8788`, full local Playwright with 56 passed and 1 intentional mobile drag skip, and a 1194 by 834 iPad smoke at `/tmp/operator-service-stage-ipad.png` with zero console warnings, zero failed responses, and zero horizontal overflow. Production API regression passed against the public hostname, including protected service-stage update and blocked private schema reads. The full production Playwright matrix passed with 56 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including service-stage floor, Timeline, and Reports checks. A live 1194 by 834 iPad smoke captured `/tmp/operator-service-stage-live-ipad.png`, verified Entrees on table P2, reported no console warnings or failed responses, and no horizontal overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, and zero pacing rules, with five configured table combinations preserved.

Eighteenth ResyOS-style iPad Notify and SMS consent pass, September 14 UTC: the public reservation flow now exposes a Resy-style Notify request when the exact requested time is unavailable, even if nearby alternate slots exist. Guests enter a name and mobile number, check an explicit text-message consent box with frequency, rates, STOP, HELP, privacy, and terms language, and create a private synthetic Notify request. The protected operator app now fetches those requests through `/api/demo/operator/notify`, counts them in the Notify queue, and lets staff mark them notified, booked, or cancelled from the iPad queue. Live SMS remains disabled until carrier approval. The static and React `/sms` proof pages now describe both website checkbox consent and START keyword consent, and `pitch/public/sms-consent-proof.png` was regenerated from the current public SMS proof page at 1280 by 2598 pixels.

The Supabase migration `20260914082500_operator_notify_requests.sql` is applied to project `tcrbfctksulrwudfmxiv`. It adds the private `reservation_demo.notify_requests` table with RLS, revokes table access from public app roles, adds the private payload helper, adds server-secret-gated `reservation_demo_notify_request` and `reservation_demo_operator_notify` RPCs, and updates reset to clear Notify requests. Direct SQL verification showed RLS enabled, no public policies, table grants only to owner roles, public RPC execution limited to the existing server-secret pattern, and the helper executable only by database owner roles.

Local verification used Cloudflare Pages dev at `http://localhost:8788` with ignored local secrets and SMS still disabled. `npm run build --prefix pitch` passed, Vitest passed 32 tests, local API regression passed including Notify create/list/status and blocked direct anon reads of `notify_requests`, and the full local Playwright matrix passed with 59 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A focused screenshot smoke captured `/tmp/reservations-notify-consent-local.png` and `/tmp/ipad-notify-queue-local.png`, verified no failed responses, no console warnings, and no horizontal overflow on the iPad view, then reset demo data.

Production verification for the Notify and consent pass, September 14 UTC: GitHub Actions run 34824348158 deployed commit a634107e0804c47729d33a91d15e6498107e67ef to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including Notify create/list/status, pacing set/list/clear, protected service-stage update, table assignment, guest profile update, table block create and clear, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, table combinations, pacing rules, and Notify requests. The full production Playwright matrix passed with 59 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new guest Notify and iPad Notify queue flow. Final protected production reset left zero bookings, zero holds, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, zero Notify requests, zero notification previews, and five configured table combinations preserved.

Twilio 30896 proof hardening, September 14 UTC: after reviewing the rejection screenshot again, the public SMS proof page was tightened so carrier reviewers can see the full absolute privacy URL, terms URL, reservation opt-in URL, program disclosure URL, and hosted screenshot URL directly in the consent proof copy instead of relying on relative links or link targets. Local build passed, Vitest passed 32 tests, and local browser smoke for `/sms/`, `/sms/privacy/`, and `/sms/terms/` confirmed HTTP 200, zero horizontal overflow, no console warnings, no page errors, and the expected compliance strings in raw built HTML. The hosted proof screenshot was regenerated as `pitch/public/sms-consent-proof.png`, 1280 by 2678 pixels, 436146 bytes, SHA256 `68544629cbd4eadff12738790e7cbbaf8422859de25206244da87151805eeedb`. SMS remains disabled until carrier approval and controlled test activation.

Production verification for Twilio 30896 proof hardening, September 14 UTC: GitHub Actions run 34825588827 deployed commit 6df8363f42d802d25281a57f1d0c62449f9c9a98 to https://res.beareberly.com. Public health returned the matching release, backend `ready`, SMS database ready, and SMS disabled. Raw production HTML fetched with a Twilio-like user agent confirmed `/sms/` contains the absolute privacy URL, terms URL, reservation URL, program disclosure URL, hosted screenshot URL, START, +1 (209) 709-4194, STOP, HELP, and message and data rates disclosure. `/sms/privacy/` contains mobile opt-in data non-sharing language, and `/sms/terms/` contains START, STOP, HELP, frequency, rate, and privacy URL disclosures. The proof image returned HTTP 200 with 436146 bytes and SHA256 `68544629cbd4eadff12738790e7cbbaf8422859de25206244da87151805eeedb`. Rendered production smoke passed for `/sms/`, `/sms/privacy/`, and `/sms/terms/` across desktop, iPad, and mobile with zero horizontal overflow and no console warnings or page errors. The campaign still needs to be edited and resubmitted in Twilio Console; SMS remains disabled until carrier approval and controlled test activation.

Nineteenth ResyOS-style iPad timed table availability pass, September 14 UTC: the protected operator app now supports Resy-style single-service table availability updates from the iPad floor controls. Hosts can choose the active floor time, set a `Block from` and `Until` window, enter a block note, and tap an open table to block only that window. The floor map now scopes occupied and blocked table states to the selected service time, while the Timeline board renders table lanes when timed blocks exist even if there are no reservations and shows the block card across the blocked window. Status copy and table accessibility labels now include the service time so staff can tell whether they are acting on 5:00, 7:30, or another dinner slot.

Local verification used Cloudflare Pages dev at `http://localhost:8788` with ignored local secrets and SMS still disabled. `npm run build --prefix pitch` passed, Vitest passed 32 tests, protected local API regression passed including the timed table-block window and blocked-table seating rejection, the focused iPad timed-block Playwright flow passed, and the full local Playwright rerun reached 57 passed with 1 intentional mobile drag skip before two unrelated transient full-run failures that each passed when rerun individually. A 1194 by 834 local iPad smoke captured `/tmp/ipad-timed-table-block-floor-local.png` and `/tmp/ipad-timed-table-block-timeline-local.png`, verified the P1 7:30 to 8:00 block on the floor and timeline, reported zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow, then reset demo data.

Production verification for the timed table availability pass, September 14 UTC: GitHub Actions run 34827512171 deployed commit c81d0718e0183391e83973683a3fd659da0631fb to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including the timed table-block window, protected service-stage update, Notify create/list/status, pacing set/list/clear, table assignment, guest profile update, waitlist create and seat, `P3+P4` combined-table seating, one-table concurrency, and blocked direct anon reads of bookings, waitlist entries, guest profiles, table blocks, table combinations, pacing rules, and Notify requests. The production full Playwright matrix reported 50 passed with 1 intentional mobile drag skip and confirmed the new timed-block test passed on desktop, iPad, and mobile, but also hit reset deadlocks and shared-state collisions in older long-flow tests; those affected flows passed in local reruns and the protected production API regression remained green. A live 1194 by 834 iPad smoke captured `/tmp/ipad-timed-table-block-floor-live.png` and `/tmp/ipad-timed-table-block-timeline-live.png`, verified P1 blocked from 7:30 to 8:00 on the floor and timeline, reported no console warnings, no page errors, no failed responses, no horizontal overflow, and no page overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, zero Notify requests, and five configured table combinations preserved.

Twilio 30896 visible checkbox proof-form pass, September 14 UTC: the rejection screenshot was reviewed again and the public SMS proof page now includes an actual visible carrier-review form with a mobile-number field and an unchecked consent checkbox instead of only quoting the checkbox copy. The static route generator renders the same form directly into `/sms/` HTML so reviewers that do not execute JavaScript still see the consent field, rates, STOP, HELP, and absolute privacy and terms URLs. The hosted proof screenshot was regenerated from the built page as `pitch/public/sms-consent-proof.png`, 1280 by 2847 pixels, 450626 bytes, SHA256 `129a876a874f6b117cf999b45fb2e1db5008889730978dd60a75500c37f19c64`. The recommended Twilio `message_flow` now points reviewers to `https://res.beareberly.com/sms/`, `https://res.beareberly.com/sms/privacy/`, `https://res.beareberly.com/sms/terms/`, and `https://res.beareberly.com/sms-consent-proof.png` with trailing slashes for direct HTTP 200 responses.

Production verification for Twilio 30896 visible checkbox proof-form pass, September 14 UTC: GitHub Actions run 34829654994 deployed commit b2b7ccc26b491891bdc92177cfa756db8f1eeb4f to https://res.beareberly.com. Public health returned the matching release, backend ready, SMS database ready, and SMS disabled. Raw production HTML confirmed `/sms/` contains `Website SMS opt-in proof form`, `type="checkbox"`, message and data rates, STOP, HELP, and absolute privacy and terms URLs. `/sms/privacy/` and `/sms/terms/` returned HTTP 200. The proof PNG returned HTTP 200, `image/png`, 450626 bytes, and matching SHA256 `129a876a874f6b117cf999b45fb2e1db5008889730978dd60a75500c37f19c64`. Rendered production smoke on a 390 by 844 viewport confirmed the proof form is visible, the checkbox is unchecked, the mobile field is visible, there is no horizontal overflow, and there are no console warnings or page errors. Browser plugin was not available, so rendered validation used Playwright. The campaign still needs to be edited and resubmitted in Twilio Console; SMS remains disabled until carrier approval and controlled test activation.

Twentieth ResyOS-style iPad reservation detail editing pass, September 14 UTC: the protected operator app now edits selected-party reservation details directly from the iPad drawer. Staff can update guest name, service date, time, party size, indoor or outdoor seating, mobile contact, and a private host note. The Supabase migration `20260914093432_operator_reservation_detail_edit.sql` is applied to project `tcrbfctksulrwudfmxiv`; it adds private `operator_note` storage, updates the protected operator list payload, and adds the server-secret-gated `reservation_demo_operator_edit` RPC with explicit anon and authenticated execute grants. Direct wrong-secret execution returns `operator_unauthorized`.

Local verification used Cloudflare Pages dev at `http://127.0.0.1:8790` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, protected local API regression passed including wrong-token rejection and persisted operator detail edits, and the full local Playwright matrix passed with 62 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A focused 1194 by 834 iPad smoke captured `/tmp/ipad-reservation-detail-editor-local.png`, verified the edited selected-party drawer, host note chip, note rehydration, zero horizontal overflow, zero page overflow, no console warnings, no failed responses, and then reset synthetic demo data to zero bookings, holds, notifications, waitlist entries, profiles, table blocks, pacing rules, and Notify requests with five configured table combinations preserved. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the reservation detail editing pass, September 14 UTC: GitHub Actions run 34831761384 deployed commit 48630b0e43f3ec51f2042f8cbb0196d8bc2d9291 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected operator reservation detail edit, wrong-token rejection, persisted host note, pacing set/list/clear, service-stage update, table assignment, guest profile update, table block create and clear, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct anon reads of private demo tables. The full production Playwright matrix passed with 62 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new selected-party detail edit flow. A live 1194 by 834 iPad smoke captured `/tmp/ipad-reservation-detail-editor-live.png`, verified the edited drawer, host note chip, note rehydration, zero horizontal overflow, zero page overflow, no console warnings, and no failed responses. Final protected production reset left zero bookings, holds, notification previews, waitlist entries, profiles, table blocks, pacing rules, and Notify requests, with five configured table combinations preserved.

Twenty-first ResyOS-style iPad floor map pass, September 14 UTC: the protected operator app now presents the Floor rail as a richer host floor plan instead of a demo grid. Dining Room and Patio maps now include room shells, zone labels, host and service landmarks, table rotation, seat-dot affordances for open tables, and occupied table cards that show guest name, table code, party size, time, status, manual service stage, and turn-risk directly on the table. Existing tap seating, drag seating, Move table touch fallback, timed blocks, waitlist seating, combined tables, selected-party drawer, Timeline, Availability, Reports, Guestbook, Notify, and Texts behavior remained unchanged.

Local verification used Cloudflare Pages dev at `http://127.0.0.1:8788` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, the focused iPad floor checks passed for selected-party seating and service-stage floor rendering, and the full local Playwright matrix passed with 62 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A 1194 by 834 local iPad smoke captured `/tmp/ipad-resy-floor-map-local.png`, verified the occupied P2 tile rendered `Floor Map Guest`, `P2 · 2 · 7:30 PM`, `SEATED · ENTREES · ON PACE`, reported zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow, then reset synthetic demo data. Browser plugin was not available, so rendered validation used Playwright.

Production verification for the ResyOS-style iPad floor map pass, September 14 UTC: GitHub Actions run 34833741043 deployed commit c3e27fa39fe14311ea03579d71525a77e3b6c1cf to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected table assignment, service-stage update, reservation detail edit, guest profile update, pacing set/list/clear, Notify create/list/status, timed table block, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct private schema reads. The full production Playwright matrix passed with 62 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new floor tile guest/name/time/service assertion. A live 1194 by 834 iPad smoke captured `/tmp/ipad-resy-floor-map-live.png`, verified the deployed P2 tile rendered `Floor Map Guest`, `P2 · 2 · 7:30 PM`, `SEATED · ENTREES · ON PACE`, reported zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, and zero Notify requests, with five configured table combinations preserved.

Twenty-second ResyOS-style iPad arrival timing pass, September 14 UTC: the protected operator app now treats arrival urgency as a first-class host signal. Reservations are labeled Early, Due now, Late, Here, Seated, Done, or Cancelled from the live service clock and reservation status, not from the manually selected floor snapshot time. The Queue row, occupied floor tile, selected-party preview, Timeline card, selected-party drawer, and Reports rail now show arrival timing and detail copy such as `12 min late`, `At this slot`, or `Entrees · On pace`. Arrival timing also contributes to the Reports rail attention count and has a dedicated `Arrival timing report` while preserving the existing service-stage report.

Local verification used Cloudflare Pages dev at `http://127.0.0.1:8788` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, local API regression passed, the focused iPad arrival tests passed, and the full local Playwright matrix passed with 65 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A 1194 by 834 local iPad smoke captured `/tmp/ipad-arrival-timing-local.png`, verified the seated floor tile, selected-party arrival timing drawer, Arrival timing report, zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow, then reset synthetic demo data. Browser plugin was not available, so rendered validation used Playwright.

Production verification for the ResyOS-style iPad arrival timing pass, September 14 UTC: GitHub Actions run 34835158279 deployed commit e3955586fc7ae98730a794fb405b0c2bc91ebcef to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected table assignment, service-stage update, reservation detail edit, guest profile update, pacing set/list/clear, Notify create/list/status, timed table block, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct private schema reads. The full production Playwright matrix passed with 65 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new frozen-clock arrival timing test that proves Late stays tied to the service clock when the floor snapshot selector changes. A live 1194 by 834 iPad smoke captured `/tmp/ipad-arrival-timing-floor-live.png` and `/tmp/ipad-arrival-timing-report-live.png`, verified the occupied P2 floor tile, selected-party Arrival timing drawer, Arrival timing report, zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, and zero Notify requests, with five configured table combinations preserved.

Twenty-third ResyOS-style iPad host command tray pass, September 14 UTC: the protected operator app now has a selected-party host command card in the iPad drawer. The card turns the existing service plan into direct lifecycle controls, with a prominent next action plus fast Check in, Seat, Move, Finish, Cancel, and Profile buttons. It keeps the Resy-style host workflow inside the selected party context so staff can check in, start seat-from-floor mode, advance service stage, finish the turn, or cancel without returning to the queue row. The existing move-table touch fallback instruction was preserved as `Drag this party or tap an open highlighted table.`

Local verification used Cloudflare Pages dev at `http://127.0.0.1:8788` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, protected local API regression passed, the focused iPad host command tray test passed, and the full local Playwright matrix passed with 68 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A 1194 by 834 local iPad smoke captured `/tmp/ipad-host-command-tray-local.png`, verified the seated command tray with `Next: Ordered`, `Mark Ordered`, lifecycle buttons, zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow, then reset synthetic demo data. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the ResyOS-style iPad host command tray pass, September 14 UTC: GitHub Actions run 34837356821 deployed commit 0c3179542f5020bacfce5e4f530c146b24cef366 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected status update, service-stage update, reservation detail edit, table assignment, guest profile update, pacing set/list/clear, Notify create/list/status, timed table block, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct private schema reads. The full production Playwright matrix passed with 68 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new selected-party host command tray flow. A live 1194 by 834 iPad smoke captured `/tmp/ipad-host-command-tray-live.png`, verified the seated host command card with `Next: Ordered`, `Mark Ordered`, Check in, Seat, Move, Finish, Cancel, and Profile controls, reported zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, and zero Notify requests, with five configured table combinations preserved.

Twenty-fourth ResyOS-style iPad speed workflow pass, September 14 UTC: the protected operator app now adds a host-speed triage strip under the queue filters and removes the second search step when booking directly from the Availability board. The triage strip surfaces Late, Due now, Here, and Unseated counts with one-tap queue filtering. The Availability board `Book this time` action now hands the exact live availability result into the Book rail, preloads the selected date, time, party size, seating area, and bookable Indoor or Patio actions, and keeps the normal server-side hold and confirm workflow for the actual reservation.

Local verification used a fresh Cloudflare Pages dev worker at `http://127.0.0.1:8810` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, protected local API regression passed, and the full local Playwright matrix passed with 71 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. The new local browser coverage verified the Availability-to-Book handoff and arrival triage filters across all projects. A 1194 by 834 local iPad smoke captured `/tmp/ipad-arrival-triage-local.png` and `/tmp/ipad-availability-handoff-local.png`, verified Late 1, Due now 1, Here 1, Unseated 3, immediate `Ready to book 2 guests at 5:00 from live availability` state, visible `Book Indoor` and `Book Patio` actions, zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow, then reset synthetic demo data. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the ResyOS-style iPad speed workflow pass, September 14 UTC: GitHub Actions run 34839561757 deployed commit 376a595ca4deab2466a14a3c61c950caa4c9a0d0 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected status update, service-stage update, reservation detail edit, table assignment, guest profile update, pacing set/list/clear, Notify create/list/status, timed table block, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct private schema reads. The full production Playwright matrix passed with 71 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile, including the new Availability-to-Book handoff and arrival triage filter coverage. A live 1194 by 834 iPad smoke captured `/tmp/ipad-arrival-triage-live.png` and `/tmp/ipad-availability-handoff-live.png`, verified Late 1, Due now 1, Here 1, Unseated 3, immediate `Ready to book 2 guests at 5:00 from live availability` state, visible `Book Indoor` and `Book Patio` actions, zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero page overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero Notify requests, zero profiles, zero table blocks, and zero pacing rules, with five configured table combinations preserved.


Twenty-fifth ResyOS-style iPad host intelligence and Timeline table-lane pass, September 14 UTC: the protected operator app now adds a live sync status panel, a host briefing card with next-action routing, and a Timeline-to-Book handoff from empty table cells. The Timeline handoff opens the Book rail with a source chip, preloads the exact service date, time, seating area, party size, and bookable action, then assigns the confirmed reservation back to that table lane so the reservation appears on the selected table row.

Local verification used Cloudflare Pages dev at `http://127.0.0.1:8814` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, protected local API regression passed, and focused iPad checks passed for the sync panel, host briefing, Timeline table-cell booking, and existing Book rail flow. The full local Playwright matrix passed with 80 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A 1194 by 834 iPad smoke captured `/tmp/ipad-timeline-empty-local.png`, `/tmp/ipad-timeline-book-source-local.png`, and `/tmp/ipad-timeline-table-assignment-local.png`; it verified the Timeline source chip, `Ready to book 2 guests at 5:00 from table 12`, the created booking on table 12's Timeline row, zero console warnings, zero page errors, zero failed responses, zero document horizontal overflow, zero document vertical overflow, and only the intended Timeline internal horizontal scroll. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.


Production verification for the host intelligence and Timeline table-lane pass, September 14 UTC: GitHub Actions run 34842843616 deployed commit 13eb34892661c0074eace39e2daf90a7264c818f to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected status update, service-stage update, reservation detail edit, table assignment, guest profile update, pacing set/list/clear, Notify create/list/status, timed table block, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct private schema reads. Focused live iPad checks passed for the sync panel, host briefing, Timeline table-cell booking, and existing Book rail flow. The full production Playwright matrix passed with 80 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A live 1194 by 834 iPad smoke captured `/tmp/ipad-timeline-empty-live.png`, `/tmp/ipad-timeline-book-source-live.png`, and `/tmp/ipad-timeline-table-assignment-live.png`; it verified the Timeline source chip, `Ready to book 2 guests at 5:00 from table 12`, the created booking on table 12's Timeline row, zero console warnings, zero page errors, zero failed responses, zero document horizontal overflow, zero document vertical overflow, and only the intended Timeline internal horizontal scroll. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, and zero Notify requests, with five configured table combinations preserved.

Twenty-sixth ResyOS-style iPad server-section pass, September 14 UTC: the protected operator floor now adds server-section badges to every table, a Server rotation card, and selected-party server context. The current implementation derives sections from table assignments without a new database migration: Andrea owns the dining window section, Marco owns the banquette wall, Sofia owns the patio rail, and Nico owns garden and counter. The card shows active covers, seated tables, open tables, upcoming parties, completed covers, turn alerts, a load-based `Suggested next` server, and a `Recent seated` note. The user supplied another Twilio rejection screenshot during this pass; it matches the already recorded Error 30896 opt-in issue, so SMS remains disabled and the next carrier action is still editing or resubmitting the campaign with the public proof URLs and screenshot evidence.

Local verification used Cloudflare Pages dev at `http://127.0.0.1:8815` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, protected local API regression passed, the focused iPad server-rotation test passed, and the full local Playwright matrix passed with 83 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A first full run exposed old table-block selectors that assumed table accessible names contained no server label; those selectors were updated, and the focused block-and-clear test then passed across desktop, iPad, and mobile before the full matrix was rerun cleanly. A rendered 1194 by 834 local iPad smoke captured `/tmp/ipad-server-rotation-local.png` and `/tmp/ipad-server-selected-local.png`, verified the Server rotation card, `Suggested next: Nico`, table labels for server Andrea and server Sofia, selected Andrea server context, zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero vertical overflow. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the iPad server-section pass, September 14 UTC: GitHub Actions run 34845343275 deployed commit 423cf2a5b98a95ec424f42c8bbb4ba219badb02c to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected status update, service-stage update, reservation detail edit, table assignment, guest profile update, pacing set/list/clear, Notify create/list/status, timed table block, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct private schema reads. The focused live iPad server-rotation test passed, and the full production Playwright matrix passed with 83 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A live 1194 by 834 iPad smoke captured `/tmp/ipad-server-rotation-live.png` and `/tmp/ipad-server-selected-live.png`; it verified the Server rotation card, `Suggested next: Nico`, table labels for server Andrea and server Sofia, selected Andrea server context, zero console warnings, zero page errors, zero failed responses, zero horizontal overflow, and zero vertical overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, and zero Notify requests, with five configured table combinations preserved. SMS remains disabled because the Twilio campaign still needs successful carrier approval after the 30896 opt-in rejection.

Twenty-seventh ResyOS-style iPad selected-party activity pass, September 14 UTC: the protected operator drawer now adds a selected-party activity timeline that brings Resy-style host history into the iPad workflow. The log summarizes the current service or lifecycle state first, then table and server assignment, arrival state, guest record, staff note, reservation creation, and sync state. The activity card sits below the manual service controls and host action buttons so Check in, Seat, Move, Finish, Cancel, Profile, and service-stage actions stay reachable before history review. A read-only Resy parity audit confirmed this moves the inspector closer to the reference staff workflow by combining activity history, guest profile context, notes, service state, and sync state in one selected-party surface.

Local verification used Cloudflare Pages dev at `http://127.0.0.1:8817` with ignored local secrets and SMS still disabled. `npm run build` passed, Vitest passed 32 tests, protected local API regression passed, the focused activity timeline test passed across desktop, iPad, and mobile, and the focused host-command, Move table fallback, and iPad Book rail regression group passed with 12 tests. The full local Playwright matrix passed with 86 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A rendered 1194 by 834 local iPad smoke captured `/tmp/ipad-activity-selected-local.png`, `/tmp/ipad-activity-timeline-local.png`, and `/tmp/ipad-activity-card-local.png`; it verified the current-first activity order with `Ordered` before `Reservation created`, table 12, Andrea server context, guest record, private note, sync state in the DOM, zero console warnings, zero page errors, zero failed responses, zero document horizontal overflow, zero document vertical overflow, zero selected-panel overflow, and zero activity-card overflow. Browser plugin was not available, so rendered validation used Playwright. Production deployment and live verification remain next.

Production verification for the selected-party activity timeline pass, September 14 UTC: GitHub Actions run 34849887648 deployed commit 09a61b0ed5e064b4a471c201cf3de03255d02108 to https://res.beareberly.com. Public health returned the matching release with backend ready, SMS database ready, and SMS disabled. Production API regression passed against the public hostname, including protected status update, service-stage update, reservation detail edit, table assignment, guest profile update, pacing set/list/clear, Notify create/list/status, timed table block, waitlist create and seat, combined table seating, one-table concurrency, and blocked direct private schema reads. The focused live selected-party activity timeline test passed across desktop, iPad, and mobile, and the full production Playwright matrix passed with 86 tests and 1 intentional mobile drag skip across desktop, iPad, and mobile. A live 1194 by 834 iPad smoke captured `/tmp/ipad-activity-selected-live.png`, `/tmp/ipad-activity-timeline-live.png`, and `/tmp/ipad-activity-card-live.png`; it verified `Ordered` before `Reservation created`, table 12, Andrea server context, guest record, private note, sync state in the DOM, zero console warnings, zero page errors, zero failed responses, zero document horizontal overflow, zero document vertical overflow, zero selected-panel overflow, and zero activity-card overflow. Final protected production reset left zero bookings, zero holds, zero notification previews, zero waitlist entries, zero profiles, zero table blocks, zero pacing rules, and zero Notify requests, with five configured table combinations preserved. SMS remains disabled because the Twilio campaign still needs successful carrier approval after the 30896 opt-in rejection.
